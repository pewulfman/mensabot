import { router as rootRouter} from './Root';
import { router as setupRouter } from './Setup';
import { router as validationRouter } from './Validations';

export {
    rootRouter,
    setupRouter,
    validationRouter,
};